{
  "name": "tokensets-api",
  "version": "1.0.8",
  "description": "Minimal wrapper around api.tokensets.com",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build --clean"
  },
  "author": "Tiago Siebler",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2"
  },
  "devDependencies": {
    "@types/node": "^14.0.24"
  }
}
